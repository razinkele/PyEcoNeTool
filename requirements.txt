# Core Shiny Framework
shiny>=1.0.0
htmltools>=0.5.0
shinyswatch>=0.4.0  # Bootstrap themes

# Network Analysis
networkx>=3.0

# Network Visualization
# razinkele fork; not on PyPI. Requires git on PATH at install time.
# The distribution was renamed pyvis -> pyvis-optimized in v4.3.1, so the
# requirement name must be pyvis-optimized; the import name is still `pyvis`.
# Tag v4.4.0 == commit bee1467e90eeedd3f6878b167d0ae21ad2807069 (tags are mutable;
# re-verify this if the tag is ever moved).
# The `shiny` env is SHARED — on laguna by ~20 apps, locally across projects —
# and holds ONE version, so this pin is effectively cross-project. Seven apps
# import pyvis (BowTie, EconetPy, EVA, MosaicSES, NiDSES, osmose, SESPy);
# AQUABC only mentions it in vis.js/error strings and is NOT a consumer.
#
# v4.4.0 verified before bumping: additive (network teardown command, debounced
# resize guard, bundled vis-network 10.0.2 -> 10.1.2) plus one breaking change,
# `require Python >=3.10` — satisfied, the server runs 3.13.14. No app hardcodes
# the bundled `lib/vis-<ver>/` path, so the vis bundle move is safe.
# Tag v4.4.0 (tags are mutable; re-verify if it is ever moved).
pyvis-optimized @ git+https://github.com/razinkele/pyvis.git@v4.4.0

# Data Processing
pandas>=2.0.0
numpy>=2.0.0

# Scientific Computing
scipy>=1.10.0

# Plotting
matplotlib>=3.7.0
seaborn>=0.12.0

# Dev / test
pytest>=8.0
hypothesis>=6.100.0
pytest-playwright>=0.5.0
