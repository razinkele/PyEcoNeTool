# Core Shiny Framework
shiny>=1.0.0
htmltools>=0.5.0
shinyswatch>=0.4.0  # Bootstrap themes

# Network Analysis
networkx>=3.0

# Network Visualization
# razinkele fork; not on PyPI. Requires git on PATH at install time.
# The distribution was renamed pyvis -> pyvis-optimized in v4.3.1, so the
# requirement name must be pyvis-optimized; the import name is still `pyvis`.
# Tag v4.3.1 == commit d84e2931024f5d91b3fc1a969ea27596d29fcdb3 (tags are mutable).
pyvis-optimized @ git+https://github.com/razinkele/pyvis.git@v4.3.1

# Data Processing
pandas>=2.0.0
numpy>=2.0.0

# Scientific Computing
scipy>=1.10.0

# Plotting
matplotlib>=3.7.0
seaborn>=0.12.0

# Dev / test
pytest>=8.0
hypothesis>=6.100.0
pytest-playwright>=0.5.0
